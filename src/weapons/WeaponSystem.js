/** PLACEHOLDER — owned by the "weapons" agent. Replace this file entirely. */
export default class WeaponSystem {
  name = 'weapons';
  priority = 30;
  async init(ctx) {}
  fixedUpdate(dt, ctx) {}
  update(dt, alpha, ctx) {}
  dispose() {}
}
