/** PLACEHOLDER — owned by the "ai" agent. Replace this file entirely. */
export default class AISystem {
  name = 'ai';
  priority = 40;
  async init(ctx) {}
  fixedUpdate(dt, ctx) {}
  update(dt, alpha, ctx) {}
  dispose() {}
}
