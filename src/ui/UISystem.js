/** PLACEHOLDER — owned by the "ui" agent. Replace this file entirely. */
export default class UISystem {
  name = 'ui';
  priority = 80;
  async init(ctx) {}
  fixedUpdate(dt, ctx) {}
  update(dt, alpha, ctx) {}
  dispose() {}
}
