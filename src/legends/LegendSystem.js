/** PLACEHOLDER — owned by the "legends" agent. Replace this file entirely. */
export default class LegendSystem {
  name = 'legends';
  priority = 50;
  async init(ctx) {}
  fixedUpdate(dt, ctx) {}
  update(dt, alpha, ctx) {}
  dispose() {}
}
