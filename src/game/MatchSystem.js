/** PLACEHOLDER — owned by the "match" agent. Replace this file entirely. */
export default class MatchSystem {
  name = 'match';
  priority = 85;
  async init(ctx) {}
  fixedUpdate(dt, ctx) {}
  update(dt, alpha, ctx) {}
  dispose() {}
}
