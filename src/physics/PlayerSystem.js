/** PLACEHOLDER — owned by the "player" agent. Replace this file entirely. */
export default class PlayerSystem {
  name = 'player';
  priority = 20;
  async init(ctx) {}
  fixedUpdate(dt, ctx) {}
  update(dt, alpha, ctx) {}
  dispose() {}
}
