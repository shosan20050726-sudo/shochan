/** PLACEHOLDER — owned by the "audio" agent. Replace this file entirely. */
export default class AudioSystem {
  name = 'audio';
  priority = 70;
  async init(ctx) {}
  fixedUpdate(dt, ctx) {}
  update(dt, alpha, ctx) {}
  dispose() {}
}
