/** PLACEHOLDER — owned by the "vfx" agent. Replace this file entirely. */
export default class VFXSystem {
  name = 'vfx';
  priority = 60;
  async init(ctx) {}
  fixedUpdate(dt, ctx) {}
  update(dt, alpha, ctx) {}
  dispose() {}
}
